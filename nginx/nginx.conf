<<<<<<< Updated upstream
events {
    worker_connections 1024;
}

http {
    upstream user-service {
        server user-service:3000;
    }

    upstream contact-service {
        server contact-service:3002;
    }

    upstream campaign-service {
        server campaign-service:3003;
    }

    upstream template-service {
        server template-service:3004;
    }

    upstream team-service {
        server team-service:3005;
    }

    upstream whatsapp-service {
        server whatsapp-service:3006;
    }

    upstream kafka-service {
        server kafka-service:3007;
    }

    upstream automation-service {
        server automation-service:3008;
    }

    upstream frontend {
        server frontend:3000;
    }

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=100r/s;
    limit_req_zone $binary_remote_addr zone=webhook_limit:10m rate=1000r/s;

    server {
        listen 80;
        server_name localhost;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # Frontend
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # User Service
        location /user-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://user-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Contact Service
        location /contact-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://contact-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Campaign Service
        location /campaign-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://campaign-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Template Service
        location /template-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://template-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Team Service
        location /team-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://team-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WhatsApp Service
        location /whatsapp-service/ {
            limit_req zone=api_limit burst=100 nodelay;
            proxy_pass http://whatsapp-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WhatsApp Webhook (higher rate limit for Meta webhooks)
        location /whatsapp-service/api/whatsapp/webhook {
            limit_req zone=webhook_limit burst=500 nodelay;
            proxy_pass http://whatsapp-service/whatsapp-service/api/whatsapp/webhook;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            # Pass raw body for signature verification
            proxy_set_header Content-Type $content_type;
        }

        # Automation Service
        location /automation-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://automation-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            client_max_body_size 50m;
        }

        # Kafka Service (internal only, but exposed for testing)
        location /kafka-service/ {
            limit_req zone=api_limit burst=20 nodelay;
            proxy_pass http://kafka-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "OK";
            add_header Content-Type text/plain;
        }
    }
}
=======
events {
    worker_connections 1024;
}

http {
    upstream user-service {
        server user-service:3000;
    }

    upstream contact-service {
        server contact-service:3002;
    }

    upstream campaign-service {
        server campaign-service:3003;
    }

    upstream template-service {
        server template-service:3004;
    }

    upstream team-service {
        server team-service:3005;
    }

    upstream whatsapp-service {
        server whatsapp-service:3006;
    }

    upstream kafka-service {
        server kafka-service:3007;
    }

    upstream frontend {
        server frontend:3000;
    }

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=100r/s;
    limit_req_zone $binary_remote_addr zone=webhook_limit:10m rate=1000r/s;

    server {
        listen 80;
        server_name localhost;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # Frontend
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # User Service
        location /user-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://user-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Contact Service
        location /contact-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://contact-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Campaign Service
        location /campaign-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://campaign-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Template Service
        location /template-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://template-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Team Service
        location /team-service/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://team-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WhatsApp Service
        location /whatsapp-service/ {
            limit_req zone=api_limit burst=100 nodelay;
            proxy_pass http://whatsapp-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WhatsApp Webhook (higher rate limit for Meta webhooks)
        location /whatsapp-service/api/whatsapp/webhook {
            limit_req zone=webhook_limit burst=500 nodelay;
            proxy_pass http://whatsapp-service/whatsapp-service/api/whatsapp/webhook;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            # Pass raw body for signature verification
            proxy_set_header Content-Type $content_type;
        }

        # Kafka Service (internal only, but exposed for testing)
        location /kafka-service/ {
            limit_req zone=api_limit burst=20 nodelay;
            proxy_pass http://kafka-service/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "OK";
            add_header Content-Type text/plain;
        }
    }
}
>>>>>>> Stashed changes
