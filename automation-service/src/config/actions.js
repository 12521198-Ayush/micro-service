/**
 * Actions configuration
 * Controls which action types are available in the flow builder
 */
export const actionsConfig = {
  add_to_group: true,
  remove_from_group: true,
  update_contact: true,
  send_email: true,
  delay: true,
  webhook: true,
  ai_response: false,
  conditional: true
};

export default actionsConfig;
